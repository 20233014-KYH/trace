# Trace · B 개발 로드맵 (10주)

2026-09-20 · 기준: 최종 기획서 + `Trace_2인_분업_가이드.pdf` · B = 윈도우 · 수집기·확장·Learn 지표·Timeline·트레이·exe·기획/디자인/발표

> 한 줄: **지금 만든 것은 전부 B 영역이다.** 남은 건 ① 서버로 보내기(batch) ② 브라우저 확장 ③ HTML 화면을 React로 옮기기 ④ 트레이. 새로 배우는 건 React 하나.

---

## 0. 지금 위치 — 있는 것을 가이드의 자리에 놓으면

| 가이드 단계 (주차) | 지금 있는 것 | 상태 | 남은 일 |
|---|---|---|---|
| Python Collector (3~4) | `3-코드/collector/` — 창·클립보드·입력/삭제/되돌리기·파일·유휴·heartbeat · **batch 전송 · 체인 해시 · 봉인 요청** (9/20) | **완성** | 실데이터로 분류표 보정 |
| 오프라인 큐 (3) | `queue.jsonl` — session/events/end 순서 보관, 옛 세션은 옛 id로 재전송 (9/20) | **완성** | — |
| 이벤트 → Learn 지표 (4·8) | `3-코드/core/derive.py` — 분당 집계·붙여넣기 해시 매칭·stats | 뼈대 완성 | 씨름 시간·건너뛰기 횟수 추가(확장의 tab 이벤트 필요). 서버 이관은 A가 하되 함수 그대로 |
| 해시 체인 (7) | `3-코드/core/chain.py` — 수집기·derive·서버 공용. 배치마다 chain_head, 서버 재계산·대조 (`3-코드/collector/dev_receiver.py`) | **완성** | A: 진짜 Backend 에서 같은 함수로 재계산 · OTS 앵커 |
| Timeline 화면 (5~6) | `3-코드/web/` — React `Timeline.jsx` · `SessionView.jsx` (9/20) | **완성** | A의 React 골격에 끼우기 · Learn 화면 |
| Browser Extension (7) | `3-코드/extension/` + `collector/bridge.py` — 탭 → 도메인 → 체인 · Learn 맥락 (9/20) | 뼈대 완성 | 실사용 테스트 · 사이트별 입력창 · page 맥락 |
| 트레이 (8) | 없음 | — | pystray 반나절 |
| 설치 exe (Could) | 없음 | — | 시간 남으면 |
| 기획·디자인·발표 | 기획안 v0.2 · 목업북 v0.3 · 전달 문서 4개 | 진행 중 | 최종 기획서에 맞춰 목업북 Learn 화면 갱신 |

**계약 3개 중 2개는 이미 내 손에 있다.** ① 이벤트 형식 = `3-코드/README.md` 4절, ② 화면이 받는 형식 = `session.json` 구조. ③ API만 이번 주에 정하면 된다 → `1-기획/2-API 계약 초안`.

---

## 1. 합의된 결정 (9/20 · A와 오프라인 합의 + 이 문서로 확정)

| # | 결정 | 코드·목업에 할 일 | 기획서에 할 일 |
|---|---|---|---|
| 1 | **Learn은 내용(맥락)을 기록한다.** 순서: 브라우저(Must) → VS Code(Should) → Word·PowerPoint(Should). 시간 부족하면 브라우저까지만 | 브라우저 확장 뼈대 완료 · **Word·PPT 완료 (9/20, `office.py`)** · VS Code 남음 | 16-4 반영 완료 |
| 2 | **해시 체인은 PC와 서버 양쪽에서 만들고 대조한다.** 배치마다 체인 해시 전송, 서버 재계산, 수신 시각 기록, 지연 수신은 증명서에 표시 | 수집기 batch에 chain_head 추가 (2주차) · 서버 재계산은 A | 13번에 한 문장 수정 |
| 3 | Proof의 입력·삭제·되돌리기 **횟수**와 붙여넣기 해시는 이벤트다 (내용 아님). 그대로 간다 | 라벨을 "브라우저(AI 사이트)에서 복사한 것과 같은 해시"로 (9/20 반영) | 12번 예시에 한 줄 추가 |
| 4 | 화면은 React, PC 앱 창(pywebview)에 담는다. 지금 HTML은 설계도 | 5주차에 React로 이관 | 없음 |
| 5 | **Learn에 교수 공개 없음.** 밖으로 나가는 건 PDF 내보내기뿐 | 목업 반영 완료 | 26번 "교수" 항목을 "학생이 내보낸 리포트·증명서를 받는다"로 |
| 6 | **"직접 입력"은 모든 창의 입력 합계** (작업 창만 세지 않음). 어느 앱에서 쳤는지는 앱별 표에서 나뉨 | derive 그대로 · `tests/README.md` 메모 | 없음 (화면 정의) |
| 7 | **Office 문서 변화는 Proof 에서도 숫자로 기록** (+n자/-m자 · 붙여넣은 자리 · 저장 통계). 텍스트는 Learn 만 | `office.py` 두 층 (9/20) | 12번 예시에 반영 완료 (9/20) |
| 8 | **Learn 의 AI 는 Qwen API (알리바바 · 교수 추천 9/20 · 최종 선택은 팀)**. 키는 서버 환경변수에만, 리포트 3회·채팅 30회 상한, Proof 는 호출 없음. 어댑터라 Claude 로 품질 비교 가능 | `core/llm.py` · 참고 서버 `/report` `/chat` (9/20) | 22번에 "Qwen API" 한 줄 (선택) |
| 9 | **리포트는 학생이 열 때 생성** (세션 종료 시 자동 아님). 안 보는 세션엔 AI 비용 0 · 채팅은 세션당 30턴 상한 | 참고 서버 GET /report 가 없으면 생성 (9/21) | 10번 문장 수정 완료 |
| 10 | **Learn 알약 위에 작은 "AI에게 물어보기" 질문칸** — 항상 떠 있고, 누르면 채팅 패널이 위로 펼쳐졌다 ✕로 닫힘. 드래그 툴바(15)와 같은 `/chat`. Proof에는 없음 | 목업 13b·13c (9/21) | 9번(Side Chat)에 한 문단 추가 완료 |
| 11 | **알약·질문칸은 항상 위(on top)** — pywebview `on_top=True · frameless · transparent · easy_drag`. 잡아서 옮길 수 있고, 클릭하면 점 하나로 줄어드는 최소 모드. 알약은 키보드 포커스를 뺏지 않음(질문칸을 클릭했을 때만). 창 모드·테두리 없는 전체창 위에는 보이고 **독점 전체화면(게임)·관리자 창 위는 제외** — 그때도 기록은 계속됨. 게임 프로세스에 끼워 넣는 오버레이 방식은 안 함(안티치트·신뢰 원칙) | 논의 (9/21) | 8주차 pywebview 창 때 반영 |

## 2. 주차별 — 내가(B) 할 일

★ = 관문. 미루지 않는다. 기능을 줄여서라도 그 주에 끝에서 끝까지.

| 주 | B가 만드는 것 | 완료 기준 | A와 맞물리는 지점 |
|---|---|---|---|
| **1** | ③ API 계약 합의(`08` 초안) · 결정 3개 합의 · 수집기에 `mode`(learn/proof)·`work_id` 넣기 · **React 1시간**(Vite 빈 화면) | `docs/api.md` 저장소에 올라감 · `python collector.py --mode proof --work W1` 이 돌아감 | A: DB 스키마 · 이벤트 수신 API |
| **2** | **batch 전송**: 5초 또는 50건마다 `POST /api/sessions/{id}/events` · 로컬 jsonl은 그대로 유지 · 큐 재전송도 batch | dev_receiver를 A의 API 형식으로 바꿔 200 OK · 서버 꺼도 큐에 쌓임 | A가 API 안 끝났으면 내 `3-코드/collector/dev_receiver.py`를 계약대로 고쳐서 먼저 맞춤 |
| **3 ★관통1** | 수집기 → A 서버 → DB 에 이벤트가 들어가는 것 확인. 오프라인 큐 → 서버 켜면 재전송 확인 | A의 DB에서 `SELECT count(*)` 가 내 콘솔 건수와 같음 | 같이 30분 |
| **4** | `derive.py`를 A에게 이관 — 함수 시그니처 `derive(events) -> session` 고정 · ~~테스트 픽스처~~ **완료 (9/20, `3-코드/tests/`)** · 씨름 시간·건너뛰기 횟수 함수 추가 | A 서버의 `GET /api/sessions/{id}` 가 내 `session.json` 과 같은 값 | A: 로그인 · Work/Session API |
| **5** | **Timeline을 React로**: `viewer.html` → `<Timeline session={..}/>` · `lanes()` SVG를 그대로 컴포넌트로 · 가짜 데이터(`sample_session.json`)로 | 로컬에서 React 화면에 샘플이 그려짐 | A: React 골격·로그인·Dashboard — **내 컴포넌트를 A의 라우터에 끼움** |
| **6 ★관통2** | Timeline이 `GET /api/sessions/{id}` 실데이터로 그려짐 | 수집 20분 → 서버 → 화면, 손 안 대고 | A: 조회 API |
| **7** | **Browser Extension (MV3)**: `tab` 이벤트(도메인·체류·분류) 두 모드 공통 · Learn 모드일 때만 `context`(AI 질문·선택 텍스트·답변 발췌) · 기본 꺼짐 토글 | claude.ai 탭 전환이 `tab{domain:"claude.ai",category:"ai"}` 로 서버에 도착 | A: 해시 체인 서버 재계산·sealing |
| **8** | Learn 지표 4개 화면 카드 · **트레이 아이콘**(pystray: 켜짐/끄기/내 기록/종료) · Learning Report 화면(A의 AI 분석 결과를 표시하는 틀) | 트레이에서 세션 시작/종료 · 지표 카드에 숫자 | A: Proof 화면 · pywebview 창 |
| **9 ★관통3** | 통합: A의 앱 창 안에서 내 Timeline·Learn 화면·트레이가 같이 동작 · 윈도우에서 처음부터 끝까지 | 설치 없이 개발 모드로 시연 가능 | 같이 |
| **10** | 파일럿(수강생 10명) · **발표 자료** — 목업북을 발표용으로 · "증명하는 것/않는 것" 문장 | 발표 | 같이 |
| + | exe (PyInstaller) | Could | A는 app |

---

## 3. 이번 주 (1주차) — 순서대로

1. **`1-기획/2-API 계약 초안` 를 A에게 보내 30분 회의** → 합의본을 `docs/api.md`로 저장소에
2. **결정 3개** 합의 — 제안: 체인 로컬 생성 ✓ · 맥은 "확장 설치 시 완전 동작" 명시 ✓ · Tier 2 제외(Retry Loop 지표도) ✓
3. ~~기획서 5곳 반영~~ **완료 (9/20)** — `0-최종 기획서.md`. A 확인만
4. ~~수집기 수정~~ **완료 (9/20)**: mode·work_id·UUID·batch·chain_head·봉인·오프라인 큐. 2주차 항목까지 끝남
5. ~~React 1시간~~ **완료 (9/20)**: `3-코드/web/` — 뷰어를 컴포넌트로 옮김. 5주차 항목이 앞당겨짐
6. 목업북 Learn 화면(13·14)을 최종 기획서에 맞게 갱신 — Context·Side Chat·Learning Report 자리 (디자인 전달용)

---

## 4. 내가 지키는 계약 — "내가 만든 데이터는 내가 화면까지"

| 계약 | 문서 | 바꾸면 알려야 할 사람 |
|---|---|---|
| ① 이벤트 한 줄 | `3-코드/README.md` 4절 | A (DB 스키마) |
| ② session 구조 | `3-코드/README.md` 4절 · `data/sample_session.json` | A (조회 API) |
| ③ API | `1-기획/2-API 계약 초안` → `docs/api.md` | 둘 다 |
| 분류표 | `collector/domains.json` | 확장과 수집기가 같은 파일을 씀 |

---

## 5. 위험 — 내 쪽에서 미리 막을 것

| 위험 | 대응 |
|---|---|
| React를 5주차에 처음 만짐 | 1주차에 1시간, 3주차에 1시간 더. `lanes()`는 순수 SVG 문자열이라 컴포넌트 전환이 쉽다 |
| 확장이 7주차에 몰림 | ~~5주차에 manifest 먼저~~ 1주차에 뼈대 완료 (9/20). 7주차엔 정확도만 |
| Learn Context 수집이 "감시"로 읽힘 | 확장의 Context는 **기본 꺼짐**, 켤 때 항목 나열, AI 도메인에서만. 첫 실행 화면(목업 02)에 그대로 |
| 키 훅이 "키로거"로 오해 | `keys.py` 파일 자체를 보여준다. 횟수만 센다는 걸 코드로 |
| 관통 1 밀림 | 3주차에 A의 API가 안 되면 **내 dev_receiver를 계약대로 고쳐서** 내 쪽은 끝냄 |
