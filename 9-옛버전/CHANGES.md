# 변경 기록 — 작품 계층 도입

> 2026-09-13 · 작성자 A
> 팀원이 `git pull` 받고 나서 이 문서를 읽으면 무엇이 어떻게 달라졌는지 알 수 있게 썼습니다.
> 코드를 안 봐도 이해되게 쓰려고 했으니, 막히는 부분 있으면 바로 물어보세요.

---

## 0. 한 줄 요약

**기록의 단위가 "세션"에서 "작품"으로 올라갔습니다.**
증명서는 이제 파일 하나가 아니라 **작품 하나에 한 장** 나옵니다.

---

## 1. 왜 바꿨나

전에는 이렇게 가정하고 있었습니다.

```
세션 하나 = 파일 하나 = 증명서 한 장
```

그런데 실제 작업은 그렇지 않습니다. 커미션 한 건이

```
러프.procreate  →  선화.procreate  →  채색.procreate
```

이렇게 파일 세 개로 나뉘고, **그 셋을 합친 것이 하나의 창작물**입니다.
논문도 원고 + 그림 + 데이터로 나뉘죠.

파일마다 증명서가 따로 나오면 의뢰인은 증명서 세 장을 받고 "이게 다 같은 그림 맞나요?"를
되물어야 합니다. 그래서 계층을 하나 더 넣었습니다.

```
작품    ← 증명서는 여기에 한 장
 └ 파일 (여러 개)
    └ 세션 (여러 번 앉아서 작업)
       └ 스냅샷 (30초마다 남기는 해시 한 줄)
```

---

## 2. 파일을 어떻게 묶는가 — 이 부분이 제일 중요합니다

**Trace는 파일 내용을 보지 않습니다.** 해시만 받으니까
`선화.procreate`와 `채색.procreate`가 같은 작품인지 **스스로 알 수 없습니다.**
그래서 사람이 정해줘야 하는데, 여기서 설계가 갈렸습니다.

### 안 한 것 — 파일 열릴 때마다 묻기

"이 파일은 이 작품 건가요?" 팝업을 매번 띄우는 방법. **쓰지 않기로 했습니다.**

1. 사흘이면 반사적으로 누르게 됩니다. 그러면 "예를 눌렀다"는 기록이 아무것도 증명 못 합니다
2. 그림 그리다 참고 이미지 열 때마다 창이 뜨면 아무도 안 씁니다
3. 기획안 9장이 최대 리스크로 꼽은 "감시 도구로 오인"이 딱 이 느낌입니다

### 한 것 — 세 겹으로 거르기

| 단계 | 하는 일 |
|---|---|
| ① 기록 시작 | **볼 앱을 먼저 고릅니다.** 고르지 않은 앱은 창 이름조차 기록 안 함 (브라우저·메신저는 기본 제외) |
| ② 작업 중 | 그 안에서 열린 파일은 **조용히 쌓입니다. 묻지 않습니다** |
| ③ 기록 종료 | 목록을 보고 **한 번만** 확인. 여기서 뺍니다 |

### "나중에 뺄 수 있으면 불리한 걸 빼버리지 않나요?"

회의에서 나올 질문이라 미리 답을 만들어 뒀습니다.

**뺀 것도 기록에 남깁니다.** 다만 이름과 내용은 공개하지 않습니다.

> 작성자가 이 작품에서 파일 1개를 뺐습니다. (9/14 18:32)

하나 뺐으면 대수롭지 않고, 열두 개 뺐으면 보는 사람이 판단합니다.
**우리는 판정하지 않습니다.** 공백 구간을 지우지 않고 빗금으로 남기는 것과
정확히 같은 구조입니다 (기획안 핵심원칙 3).

---

## 3. chain.json 이 이렇게 바뀌었습니다

### 전

```json
{
  "sessions": [
    { "session_id": "s1", "label": "직접 작성", "snapshots": [ ... ] }
  ]
}
```

### 후

```json
{
  "works": [
    {
      "work_id": "w2",
      "title": "재이 커미션",
      "created_at": "2026-09-13T11:20:00",

      "files": [
        { "name": "러프.procreate", "first_seen": "2026-09-13T11:20:00" },
        { "name": "선화.procreate", "first_seen": "2026-09-13T16:10:00" },
        { "name": "채색.procreate", "first_seen": "2026-09-14T10:00:00" }
      ],

      "excluded": [ { "at": "2026-09-14T18:32:00" } ],

      "sessions": [
        {
          "session_id": "w2-s1",
          "file_name": "러프.procreate",
          "label": "러프",
          "started_at": "2026-09-13T11:20:00",
          "excluded": false,
          "snapshots": [ ... 전과 같음 ... ]
        }
      ]
    }
  ]
}
```

바뀐 것만 짚으면:

- 최상위가 `sessions` → **`works`**
- 작품마다 `files` (파일 목록) 과 `excluded` (뺀 사실만, **이름 없음**) 이 생김
- 세션에 `file_name` 과 `excluded` 가 생김
- **스냅샷 안쪽은 하나도 안 바뀌었습니다.** 해시 계산 방식 그대로입니다

> ⚠️ 예전 구조 파일도 그냥 열립니다. `app.py`의 `load_chain()`이 `sessions`가
> 최상위에 있으면 **"이전 기록"이라는 작품 하나로 감싸서** 돌려줍니다.
> 그래서 `git pull` 받고 바로 실행해도 기존 chain.json이 안 깨집니다.

---

## 4. app.py — 무엇이 새로 생겼나

### 새 함수 `work_root(work)` ← **이게 이번 변경의 핵심**

```python
def work_root(work):
    tails = [s["snapshots"][-1]["hash"] for s in work["sessions"] if s["snapshots"]]
    return hashlib.sha256("|".join(tails).encode("utf-8")).hexdigest()
```

**왜 필요한가:** 세션마다 체인이 따로 돕니다 (각자 GENESIS부터 시작).
그런데 증명서는 작품 하나에 한 장이니, **작품 전체를 대표하는 값 하나**가 있어야 합니다.

그래서 각 세션의 **마지막 해시**를 시간 순서대로 이어 붙여 한 번 더 SHA-256을 겁니다.
세션이 하나라도 바뀌면 이 값이 달라집니다.

이 값이 **증명서에 찍히고 외부 앵커(OpenTimestamps)에 고정되는 값**입니다.
화면에서 `작품 루트 해시`라고 적힌 게 이겁니다.

### 새 함수 `summarize_files(work)`

파일별로 묶어서 "어느 파일에 몇 분 썼나"를 냅니다. 검증 화면의 파일 띠가 이걸 씁니다.

### 새 함수 `summarize_work(work, files_summary)`

작품 전체 합계. **뺀 세션은 숫자에서 빼고** 계산합니다.

### `/api/snapshot` 이 받는 것이 늘었습니다

```json
{
  "work_id": "w123",           ← 새로 추가
  "work_title": "재이 커미션",   ← 새로 추가
  "file_name": "러프.procreate", ← 새로 추가
  "session_id": "s1757...",
  "content": "...",
  "events": { "typed": 0, "deleted": 0, "pasted": 0, "undo": 0 }
}
```

작품이 없으면 만들고, 파일 목록에 없는 파일이면 조용히 올립니다. **묻지 않습니다.**

### 새 API `/api/exclude` (POST)

파일을 작품에서 뺍니다.

```json
{ "work_id": "w123", "file_name": "참고_포즈모음.psd" }
```

하는 일:
1. `files` 목록에서 그 이름을 **지웁니다** (이름이 파일에 안 남습니다)
2. 그 파일의 세션에 `excluded: true`를 달고 `file_name`을 `null`로 바꿉니다
3. `excluded` 에 `{"at": 시각}` 만 추가합니다 — **이름 없이 사실만**

체인 자체는 건드리지 않습니다. 지우면 그게 더 수상하니까요.

### `/api/verify` 가 돌려주는 것

작품마다 이런 게 붙어서 나옵니다.

| 필드 | 뜻 |
|---|---|
| `valid` | 이 작품의 모든 세션이 체인과 맞는가 |
| `root_hash` | 작품 루트 해시 |
| `summary` | 작업 시간·파일 수·세션 수·공백·붙여넣기 등 합계 |
| `files_summary` | 파일별 시간·분량·캡처 수 |

---

## 5. seed.py — 새 구조로 데모를 만듭니다

`python seed.py` 하면 작품 **세 개**가 붙습니다. 이름만 봐도 무엇을 보여주는 예시인지 알게 지었습니다.

| 작품 | 내용 |
|---|---|
| **데모 1 — 처음부터 쓴 글** | 파일 1개(에세이.docx) · 세션 3개(사흘) · 1시간 1분 · 1,054자 · 붙여넣기 **0%** · 되돌리기 71 |
| **데모 2 — 붙여넣기가 많은 글** | 파일 1개(에세이.docx) · 세션 1개 · **5분** · 1,238자 · 붙여넣기 **95%** · 되돌리기 0 |
| **데모 3 — 파일 세 개짜리 그림** | 파일 3개 · 세션 3개 · **7시간 58분** · 뺀 파일 1개 |

### 데모 1과 2는 일부러 대조되게 만들었습니다

기획안 11.5가 B에게 시킨 것이 "① 20분 직접 쓴 글 ② 통째로 붙여넣은 글 —
**두 곡선이 다르게 보이는지 확인**"인데, 전에는 그 둘이 한 작품 안의 세션 2개로 섞여 있어
나란히 비교할 수가 없었습니다. 그래서 **다른 작품으로 갈라놨습니다.**

| | 데모 1 | 데모 2 |
|---|---|---|
| 시간 | 1시간 1분 | **5분** |
| 분량 | 1,054자 | 1,238자 |
| 붙여넣기 | 0% | **95%** |
| 되돌리기 | 71회 | **0회** |
| 고쳐 쓴 캡처 | 43% | **9%** |
| 곡선 | 오르내린다 | 거의 직선 |

**데모 2가 글자는 더 많은데 시간은 1/12입니다.** 기획안 6.2의 그림 두 개
("탐색이 있는 과정 / 없는 과정")를 데이터로 만든 것입니다.

> ⚠️ 제목에 "AI"라는 말은 넣지 않았습니다. "붙여넣기가 많다"는 **관찰된 사실**이지만
> "AI로 썼다"는 **판정**이기 때문입니다. 기획안 핵심원칙 1번을 제목에서도 지킵니다.
> 어느 쪽이 AI인지 우리는 말하지 않습니다.

곡선이 오르내리는 정도는 `make_steps()` 의 `dip_rate` 로 조절합니다
(데모 1은 0.38~0.46, 데모 2는 0.04).

`--reset`은 전과 똑같이 동작합니다 (목록 보여주고 `yes` 받아야 진행, 사본 남김).

### ⚠️ 여기서 실제로 한 번 걸렸던 함정

처음엔 데모 캡처를 **10~20분 간격**으로 만들었는데, 작업 시간이 **0분**으로 나왔습니다.

원인은 `app.py`의 이 줄입니다.

```python
IDLE_THRESHOLD = 180   # 초 (3분)
```

스냅샷 간격이 3분보다 길면 "자리를 비웠다"로 셉니다.
10분 간격이면 **전부 공백**이 되니 작업 시간이 0이 되는 거죠.

→ 실제 수집기처럼 **2분 간격**으로 만드는 `make_steps()` 생성기를 새로 넣었습니다.
데모 데이터를 손으로 고칠 일이 있으면 **간격을 3분 넘기지 마세요.**

---

## 6. 프런트엔드 (index.html · script.js · style.css)

- 화면이 **작품 카드** 단위로 그려집니다. 카드 하나 안에 파일 띠 → 뺀 파일 안내 → 작품 루트 해시 → 세션별 편집 곡선 순
- 파일 띠의 **길이는 "그 파일에 쓴 시간"** 입니다. 밖에서 들어온 게 있으면 막대를 물들이지 않고 **이름 앞에 주황 점**을 찍습니다
  (막대 전체를 주황으로 칠했더니 "이 파일이 통째로 외부에서 왔다"로 읽혀서 바꿨습니다)
- 작성 영역에 **작품 이름 / 파일 이름** 입력칸이 생겼습니다. 같은 작품 이름으로 다시 시작하면 그 작품에 이어서 쌓입니다
  (제목으로 `work_id`를 만듭니다 — 프로토타입이라 그렇고, 실제 제품이면 작품 목록에서 고르게 해야 합니다)

---

## 7. 같이 고친 버그 3개

### ① 화면 카운터가 30초마다 0으로 돌아가던 것

`sendSnapshot()`이 서버로 보낸 뒤 `events = blankEvents()`로 비우는데,
**화면에 보여주는 숫자까지 같이 비워졌습니다.**

→ `events`(지난 30초의 변화량, 서버로 감)와 `totals`(세션 누적, 화면에 뜸)를 나눴습니다.

### ② 같은 파일의 글자 수가 두 번 세어지던 것

세션 1에서 660자까지 쓰고 세션 2에서 1,688자로 끝냈으면 그 파일의 분량은 **1,688자**입니다.
그런데 세션별 최종값을 더해서 **2,348자**로 나왔습니다.

→ 파일별로 **마지막 세션의 마지막 값**을 그 파일의 분량으로 잡고, 파일끼리만 더합니다.

### ③ 예전 기록이 분량 0으로 뜨던 것

예전 세션에는 `file_name`이 없어서 파일 목록이 비었고, 그래서 분량이 0이 됐습니다.

→ 예전 구조를 감쌀 때 `(파일 이름이 없던 기록)`이라는 파일을 하나 만들어 줍니다.

---

## 8. 받고 나서 이렇게 확인해 보세요

```bash
git pull
```

```bash
conda activate cp
```

```bash
cd ~/dev/trace && python app.py
```

브라우저에서 `127.0.0.1:5001`

데모 데이터가 없으면 **새 터미널 탭**(⌘T)에서:

```bash
cd ~/dev/trace && python seed.py
```

### 눈으로 확인할 것

1. 초록 **"이 기록은 변조되지 않았습니다"**
2. **데모 1과 데모 2의 곡선이 확연히 다른가** — 1은 오르내리고 2는 한 번 튄 뒤 평평
3. **데모 3** 카드 — 파일 띠 3줄 (러프 1시간 12분 / 선화 2시간 38분 / 채색 4시간 8분), 합계 7시간 58분. 러프 이름 앞에 **주황 점**(외부 반입 1)
4. **"작성자가 이 작품에서 파일 1개를 뺐습니다"** 안내
5. **작품 루트 해시** 64자리

### 조작 실패 시연 (발표 3~4컷)

`data/chain.json`을 열어 아무 `content_hash`의 첫 글자를 바꾸고 저장 →
**"다시 검증"** 클릭 → 빨강 + **어느 파일의 몇 번 스냅샷**에서 끊겼는지 표시됩니다.

되돌릴 때:

```bash
cd ~/dev/trace && cp data/chain.demo-good.json data/chain.json
```

> `data/`는 GitHub에 안 올라갑니다(각자 로컬에서 계속 바뀌어서 충돌 나니까요).
> 그래서 `chain.demo-good.json`은 **A의 노트북에만** 있습니다.
> 발표를 다른 노트북으로 하면 `python seed.py`로 다시 만들거나 파일을 따로 챙겨야 합니다.

---

## 9. 아직 안 된 것 · 회의에서 정할 것

### ① 작성자 자기신고 — 기획안에 없던 기능입니다

화면 설계에는 넣었습니다. 붙여넣기/외부 반입 **직후에** "어디서 가져왔나요?"를 묻고
(`AI 초안` / `인용` / `내 예전 원고` / `표시 안 함`), 그 답을 체인에 묶는 기능입니다.

- **왜 넣었나:** "무엇이 AI인가"를 서비스가 판정하지 않으면서 보는 사람 질문에 답하는 유일한 길
- **근거:** 기획안 4.3의 "AI 초안을 붙여넣고 편집 → 드러남 → **AI 초안 + 편집도 정직한 증명서**"
- **아직 코드에는 없습니다.** 합의되면 `events` 옆에 `declared` 필드를 넣으면 됩니다

### ② 앵커를 언제 거는가

- **세션이 끝날 때마다** 걸어야 "그 시각에 있었다"가 강해집니다. 몰아서 걸면 시각 증명이 약해집니다
- **작품 루트**는 증명서 낼 때 한 번 더
- 기획안 4.1은 "세션 종료 시"만 적혀 있어서 작품 루트 앵커는 새로 정해야 합니다

### ③ 체인 검증이 기획안과 다릅니다 — 발표에서 질문받기 쉬운 지점

기획안 4.2는 "중간 스냅샷 하나만 바꿔도 **이후가 전부 어긋난다**"고 되어 있습니다.

그런데 `app.py`의 `verify()`는 이렇게 되어 있습니다.

```python
expected = chain_hash(prev, snap["time"], snap["content_hash"], snap["events"])
if expected != snap["hash"] and broken_at is None:
    broken_at = snap["seq"]
prev = snap["hash"]        # ← 다시 계산한 값이 아니라 "파일에 적힌 값"을 쓴다
```

마지막 줄 때문에 **고친 그 한 줄만** 깨지고 다음 줄부터는 멀쩡하게 통과합니다.

- `prev = expected` 로 바꾸면 기획안대로 이후가 전부 깨집니다
- 지금 화면 설계는 **코드 쪽(한 줄만 깨짐)** 에 맞춰 그려 뒀습니다
- 어느 쪽으로 통일할지 정해야 합니다. **일부러 안 고치고 뒀습니다** — 둘 중 뭐가 맞는지는 팀이 정할 일이라서요

> 참고로 위조자가 이후 해시를 전부 다시 계산하면 한 줄만 깨지는 문제도 사라집니다.
> 그때는 **외부 앵커에 고정된 작품 루트 해시**와 어긋나서 걸립니다.
> 다만 앵커가 이번 프로토타입 범위 밖이면 발표에서 그걸 시연할 수는 없습니다.

---

## 10. 화면 설계

전체 화면 10장은 여기 있습니다 (창작자 5장 + 검증자 4장 + 증명서 1장):

**https://claude.ai/code/artifact/2a6bae09-b4f8-4e3f-913d-6acba501ebed**

원본 파일은 `design/*.dc.html` 입니다.

> **코드와 화면 설계는 일부러 다릅니다.**
> 화면 설계는 **쓰던 도구 위에 떠 있는 상주 프로그램**(수집기 B, 로드맵 W10 이후)을 그린 것이고,
> 이번 주 Flask 프로토타입은 강의자료 STEP 1 범위라 **웹 에디터**로 갑니다.
> 기획안 4.1에 수집기 A(웹)와 B(데스크톱)가 이미 나뉘어 있으니 억지가 아닙니다.
