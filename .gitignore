# 비밀값 (API 키 등). 지금은 안 쓰지만 실수 방지용
.env

# "데이터베이스" — 각자 로컬에서 만들어지는 기록.
# 올리면 두 사람이 같은 파일을 동시에 고쳐 충돌이 납니다.
# 팀원은 `python seed.py` 로 데모 데이터를 다시 만들면 됩니다.
data/

# 파이썬이 자동으로 만드는 캐시
__pycache__/
*.pyc

# 편집기 / 맥 개인 설정
.vscode/
.DS_Store

# 디자인 캔버스 빌드 결과물 (2MB). 원본은 design/*.dc.html 이다
design/jaguk-screens.html
