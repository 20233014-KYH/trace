# -*- coding: utf-8 -*-
"""Trace 기획안 PDF 생성기

  python docs/build_proposal.py   →  docs/Trace_기획안.pdf

서식은 docs/pdf_style.py 에 있다. 분업 가이드(build_plan.py)와 같이 쓴다.

★ 이 문서에 쓰는 숫자는 전부 출처가 있거나 우리가 직접 측정한 것이다.
  지어낸 숫자는 없다. 아직 안 한 것은 '미실시'라고 적는다.
"""
import os
import sys

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))
from pdf_style import (  # noqa: E402
    INK, MUTED, FAINT, RULE, PANEL, ORANGE, GREEN, BLUE, RED, CW,
    st, P, sp, rule, code_block, callout, box, arrow, table, make_doc)
from reportlab.platypus import CondPageBreak, Paragraph  # noqa: E402

F = []

# ══════════════════════════════════════════════════════
# 표지
# ══════════════════════════════════════════════════════
F += [sp(44)]
F += [P("창작·학습 과정 기록 서비스", "label")]
F += [Paragraph('<font size="32">Trace</font>', st["h1"]), sp(3)]
F += [P("기획안 v0.3", "h2")]
F += [P("창의프로젝트1 · 2026-2학기 · 박재완 교수 · 2인 1팀 (김용현 · 팀원)", "muted")]
F += [rule(INK, 1.4)]

F += [P("<font color='#171717' size='12'>결과물만 보고는 어떻게 만들었는지 알 수 없다. "
        "Trace 는 만드는 과정을 자동으로 기록해, 학생이 스스로 돌아보게 하고 "
        "필요할 때 그 기록이 고쳐지지 않았음을 보일 수 있게 한다.</font>", "lead")]

F += box("한 줄로", [
    ("누구를 위해", "과제·작품을 만드는 학생과, 그 과정을 보고 싶은 교수"),
    ("무엇을", "작업하는 동안 무엇을 했는지 자동으로 기록"),
    ("어떻게", "PC 에 상주하는 프로그램이 창·복사·붙여넣기·타자 횟수를 남긴다"),
    ("왜", "과정을 볼 수 없으면 학생은 회고할 수 없고, 교수는 확인할 수 없다"),
], BLUE)
F += [sp(12)]

F += callout("이 서비스가 아닌 것",
             "<font color='#171717'>AI 사용을 판정하는 도구가 아닙니다.</font> "
             "점수도, 등급도, ‘AI 확률 %’ 도 계산하지 않습니다. "
             "그런 판정은 틀릴 때 학생에게 돌이킬 수 없는 피해를 주고, "
             "실제로 대학들이 그 이유로 AI 탐지기를 껐습니다.<br/><br/>"
             "Trace 는 <font color='#171717'>일어난 일을 기록</font>할 뿐이고, "
             "해석은 학생과 교수가 합니다.", ORANGE)

# ══════════════════════════════════════════════════════
F += [CondPageBreak(200)]
F += [rule()]
F += [P("1. 누구를 위해 — 타깃", "h2")]

F += table(
    ["", "누구", "이들의 상황"],
    [["주 타깃", "과제·작품을 만드는 <b>학생</b>",
      "AI 를 쓰긴 쓰는데, 끝나고 나면 내가 뭘 했는지 기억이 안 난다. "
      "의심받을 때 보여줄 것이 결과물뿐이다."],
     ["함께 쓰는 사람", "그 과제를 보는 <b>교수·조교</b>",
      "결과물만으로는 과정을 알 수 없다. 탐지기는 못 믿겠고, "
      "학생마다 붙잡고 물어볼 수도 없다."],
     ["나중에", "포트폴리오를 내는 사람, 커미션 작가",
      "확장 단계. 지금은 범위 밖."]],
    [82, 150, 271])

F += [P("타깃을 바꿨습니다 — 그 이유", "h3")]
F += [P("처음에는 <font color='#171717'>커미션 작가</font>가 타깃이었습니다. "
        "“AI 쓴 거 아니냐”는 의심을 받는데 결백을 증명할 방법이 없다는 문제였습니다.", "body")]
F += [P("박재완 교수님 인터뷰에서 방향이 바뀌었습니다.", "body")]
F += [Paragraph('<para leftIndent="14" borderColor="#e0dcd6" borderWidth="0" >'
                '“교육에 매우 효과적일 것 같다. 예술 쪽엔 별로다. '
                '학교에서 잘 써먹을 것 같다.”</para>', st["quote"])]
F += [P("그래서 <font color='#171717'>학습자와 교육자</font>로 옮겼습니다. "
        "동시에 문제를 보는 각도도 바뀌었습니다 — "
        "<font color='#171717'>“의심받는다”가 아니라 “내 과정을 돌아볼 수 없다”</font>입니다. "
        "의심은 가끔 일어나지만, 회고가 안 되는 것은 <font color='#171717'>매번</font> 일어납니다.", "body")]

# ══════════════════════════════════════════════════════
F += [CondPageBreak(230)]
F += [rule()]
F += [P("2. 무슨 문제인가", "h2")]

F += [P("문제 ① 학생이 자기 학습 과정을 돌아볼 수 없다", "h3")]
F += [P("과제를 내고 나면 남는 것은 결과물 하나뿐입니다. "
        "어디서 막혔는지, 어디서 AI 에 물어봤는지, 그 답을 그대로 썼는지 고쳐 썼는지 — "
        "<font color='#171717'>아무것도 남지 않습니다.</font> "
        "그래서 다음 과제에서 같은 자리에서 또 막힙니다.", "body")]
F += table(
    ["근거", "내용", "비고"],
    [["MIT Media Lab (2025)",
      "학생 54명 대상 EEG 실험. LLM 을 쓴 집단은 <b>78% 가 자신이 방금 쓴 에세이를 "
      "한 문장도 인용하지 못했다.</b>",
      f'<font color="{ORANGE.hexval()}">arXiv 공개본 · 동료심사 전</font>'],
     ["교수 인터뷰 (2026-09)",
      "“교육에 효과적일 것 같다. 학교에서 잘 써먹을 것 같다.”", "1건 · 표본 적음"]],
    [130, 270, 103])
F += [P("첫 줄은 <font color='#171717'>“AI 를 쓰면 나빠진다”가 아니라 "
        "“쓰고 나면 내가 뭘 했는지 남지 않는다”</font>는 뜻으로 읽었습니다. "
        "Trace 가 겨냥하는 지점이 정확히 이것입니다.", "small")]

F += [P("문제 ② 과정을 보여줄 방법이 없다", "h3")]
F += [P("교수가 과정을 보고 싶어도 볼 방법이 없고, 학생이 보여주고 싶어도 보여줄 것이 없습니다. "
        "AI 탐지기가 그 자리를 대신하려 했지만 실패했습니다.", "body")]
F += table(
    ["근거", "내용"],
    [["Liang 외 (2023) · Patterns",
      "GPT 탐지기가 비원어민 영어 글을 원어민 글보다 훨씬 자주 ‘AI’ 로 잘못 판정."],
     ["Vanderbilt University (2023.08)",
      "오탐으로 인한 피해를 이유로 Turnitin 의 AI 탐지 기능을 껐다."],
     ["Grammarly Authorship",
      "판정 대신 <b>‘어떻게 썼는지 과정을 보여주는’</b> 방향으로 기능을 냈다."],
     ["EU AI Act 50조 (2026.08.02 적용)",
      "AI 생성물에 표시 의무. ‘무엇을 어떻게 썼는가’ 를 밝히는 흐름이 제도로 온다."]],
    [150, 353])
F += callout("여기서 우리가 내린 결론",
             "<font color='#171717'>판정은 하지 않는다.</font> "
             "탐지기가 실패한 이유가 판정이었기 때문입니다. "
             "대신 <font color='#171717'>과정을 기록</font>합니다. "
             "Grammarly 가 간 방향이 같고, 제도도 그쪽으로 가고 있습니다.", GREEN)

# ══════════════════════════════════════════════════════
F += [CondPageBreak(240)]
F += [rule()]
F += [P("3. 무엇을 하는가 — 두 개의 모드", "h2")]
F += [P("같은 기록을 쓰지만 목적이 완전히 다릅니다. "
        "<font color='#171717'>이 둘을 나눈 것이 이 기획의 핵심 결정입니다.</font>", "body")]

F += table(
    ["", "Learn Mode", "Proof Mode"],
    [["누가 본다", "학생 자신", "교수 · 제3자"],
     ["무엇을 위해", "내 과정을 돌아보려고", "고쳐지지 않았음을 보이려고"],
     ["기록을 고칠 수 있나", "<b>고치고 지울 수 있다</b> (지운 자리는 표시로 남는다)",
      "<b>봉인된다.</b> 고칠 수 없다"],
     ["무결성이 필요한가", "필요 없다", "반드시 필요하다"],
     ["언제 쓰나", "<b>매일</b>", "제출할 때만"],
     ["우선순위", f'<font color="{GREEN.hexval()}">Must</font>',
      f'<font color="{ORANGE.hexval()}">Should</font>']],
    [110, 200, 193])

F += callout("왜 Learn 이 Must 이고 Proof 가 Should 인가",
             "회고는 <font color='#171717'>매일</font> 필요하지만, 남을 설득할 일은 "
             "<font color='#171717'>가끔</font> 생깁니다. "
             "그리고 Learn Mode 에는 해시 체인이 아예 필요 없습니다 — "
             "내가 보려고 쓰는 기록을 내가 위조할 이유가 없으니까요.<br/><br/>"
             "무결성은 <font color='#171717'>남을 설득할 때만</font> 필요한 장치입니다. "
             "그래서 매일 쓰는 기능을 먼저 만들고, 봉인은 그 위에 얹습니다.", MUTED)

# ══════════════════════════════════════════════════════
F += [CondPageBreak(250)]
F += [rule()]
F += [P("4. 유저 플로우", "h2")]

F += [P("학생 — 평소", "h3")]
F += box("", [
    ("① 켠다", "PC 를 켜면 Trace 가 자동으로 시작된다. 학생은 신경 쓰지 않는다."),
    ("② 그냥 한다", "<b>Trace 안에서 작업하지 않는다.</b> 워드·PPT·포토샵·VS Code 등 "
                   "평소 쓰던 프로그램을 그대로 쓴다."),
    ("③ 끝낸다", "작업을 마치면 세션이 닫힌다."),
    ("④ 돌아본다", "Learn Mode 에서 오늘 과정을 시간 막대로 본다."),
], BLUE, label_w=90)
F += [sp(10)]
F += callout("②가 이 서비스의 전제입니다",
             "학생은 Trace 를 <font color='#171717'>쓰는 것이 아니라 켜 두는 것</font>입니다. "
             "작업 도구를 바꾸라고 요구하는 순간 아무도 안 씁니다. "
             "그래서 특정 앱의 플러그인이 아니라 <font color='#171717'>OS 층에서</font> 기록합니다. "
             "포토샵이든 워드든 가리지 않는 이유입니다.", ORANGE)

F += [P("학생 — 제출할 때", "h3")]
F += box("", [
    ("⑤ 고른다", "이 과제에 해당하는 세션들을 학생이 직접 고른다."),
    ("⑥ 봉인한다", "루트 해시를 확정하고 증명서를 만든다. 이후로는 못 고친다."),
    ("⑦ 낸다", "기록 + 증명서를 한 묶음으로 제출한다."),
], MUTED, label_w=90)
F += [sp(10)]
F += [P("⑤가 중요합니다 — <font color='#171717'>무엇을 공유할지 학생이 정합니다.</font> "
        "기록은 전부 학생 PC 에 먼저 쌓이고, 내보내는 것은 학생의 결정입니다.", "body")]

F += [P("교수", "h3")]
F += box("", [
    ("① 연다", "학생이 낸 것을 연다."),
    ("② 확인한다", "‘만들어진 뒤 고쳐지지 않았음’ 이 자동으로 검사된다."),
    ("③ 본다", "시간 막대로 과정을 본다. <b>기록이 없는 구간도 그대로 보인다.</b>"),
], BLUE, label_w=90)
F += [sp(10)]
F += [P("③에서 <font color='#171717'>기록 없는 구간을 숨기지 않는 것</font>이 "
        "이 서비스의 태도입니다. 없는 것을 있는 척하지 않습니다.", "body")]

# ══════════════════════════════════════════════════════
F += [CondPageBreak(250)]
F += [rule()]
F += [P("5. 어떤 기능을, 왜 만드는가", "h2")]
F += [P("기능마다 <font color='#171717'>“어떤 문제 때문에 나왔는지”</font>를 붙였습니다. "
        "이유를 못 대는 기능은 넣지 않았습니다.", "body")]

F += table(
    ["기능", "왜 이게 필요한가"],
    [["활성 창 기록",
      "“언제 무엇을 보고 있었나”가 과정의 뼈대다. 도구를 가리지 않으려면 "
      "앱 플러그인이 아니라 OS 층이어야 한다."],
     ["클립보드 — 길이와 해시만",
      "AI 답변을 그대로 옮겼는지 보려면 <b>같은 글인지</b> 비교해야 한다. "
      "그런데 원문을 저장하면 학생의 글을 우리가 갖게 된다. "
      "→ <b>해시</b>(SHA-256)만 남기면 <b>내용을 안 보고도</b> 같은지 알 수 있다. 해시는 어떤 글이든 64자리 값 하나로 바꾸며, 값에서 원래 글로는 되돌릴 수 없다."],
     ["붙여넣기 감지 (키 입력)",
      "복사는 클립보드를 바꾸지만 <b>붙여넣기는 아무것도 바꾸지 않는다.</b> "
      "클립보드만 봐서는 원리적으로 못 잡는다. 그래서 키를 봐야 한다."],
     ["타자·삭제 횟수",
      "“직접 썼나, 붙였나”의 비율을 알려면 필요하다. "
      "<b>어떤 키인지는 보지 않고 횟수만</b> 센다."],
     ["파일 저장 감지",
      "작업 파일이 언제 바뀌었는지 알면, <b>기록이 없는 시각에 파일이 바뀐 것</b>이 드러난다. "
      "→ 수집기를 안 켠 구간을 숨길 수 없게 된다."],
     ["해시 체인",
      "기록이 그냥 파일이면 <b>메모장으로 열어 고치면 그만</b>이다. "
      "그러면 “과정을 기록했다”는 말에 의미가 없다."],
     ["작품 루트 해시",
      "하나의 작품은 여러 날, 여러 기기, 여러 파일에 걸친다. "
      "그래도 <b>증명서는 작품당 한 장</b>이어야 한다."],
     ["기록 없는 구간 표시",
      "안 켜고 한 작업은 잡을 수 없다. 그렇다면 <b>‘여기는 기록이 없다’고 밝히는 것</b>이 "
      "유일하게 정직한 처리다."],
     ["Learn / Proof 분리",
      "회고용 기록까지 봉인하면 학생이 자기 기록을 정리할 수 없다. "
      "봉인은 남을 설득할 때만 필요하다."]],
    [122, 381])

F += [P("복사와 붙여넣기의 해시가 같으면", "h3")]
F += [P("<font color='#171717'>“AI 창에서 복사한 것을 그대로 옮겼다”</font>가 드러납니다. "
        "내용을 하나도 보지 않고 알아낸 것입니다. 실제로 측정한 기록입니다:", "body")]
F += code_block([
    "16:07:11   copy    81 chars   832b9dcc760c...   (Claude)",
    "16:07:19   window  Microsoft PowerPoint",
    "16:07:21   paste   81 chars   832b9dcc760c...   -> PowerPoint",
], "해시가 같습니다. 원문은 어디에도 저장되지 않았습니다.")

# ══════════════════════════════════════════════════════
F += [CondPageBreak(230)]
F += [rule()]
F += [P("6. 어떻게 기록하는가 — 수집 범위", "h2")]
F += table(
    ["층", "무엇을", "이번 학기"],
    [["Tier 0 — OS",
      "활성 창 · 클립보드 길이와 해시 · 붙여넣기 · 타자와 삭제 횟수 · 파일 저장 · 유휴",
      f'<font color="{GREEN.hexval()}">Must · 이미 동작</font>'],
     ["Tier 1 — 브라우저 확장",
      "지금 보는 사이트의 도메인 · 머문 시간 · 분류(학습자료 / AI / 기타)",
      f'<font color="{ORANGE.hexval()}">Should</font>'],
     ["Tier 2 — VS Code 확장",
      "코드 수정 · 실행 · 오류 · 커밋",
      f'<font color="{RED.hexval()}">Won’t (이번 학기)</font>'],
     ["Tier 3 — 앱별 플러그인",
      "포토샵 등 앱 내부 세부 동작",
      f'<font color="{RED.hexval()}">Won’t</font>']],
    [140, 250, 113])
F += [P("Tier 3 를 하지 않는 이유는 분량 때문만이 아닙니다. "
        "앱마다 플러그인을 만들기 시작하면 <font color='#171717'>도구를 가리지 않는다는 전제</font>가 "
        "깨집니다. 우리가 지원하는 앱만 쓸 수 있는 서비스가 됩니다.", "body")]

F += [P("작동 원리 — 해시 체인", "h3")]
F += [P("각 기록의 해시를 만들 때 <font color='#171717'>바로 앞 기록의 해시를 "
        "입력값에 함께 넣습니다.</font>", "body")]
F += code_block([
    "h(i) = SHA256( h(i-1) || time || content hash || events )",
], None)
F += [P("그래서 3번째 줄을 몰래 고치면 3번의 해시가 달라지고, "
        "4번은 3번의 해시를 입력값으로 썼으므로 4번도 어긋나고, "
        "<font color='#171717'>그 뒤가 도미노처럼 전부 어긋납니다.</font> "
        "한 줄만 고치고 넘어가는 것이 불가능해집니다. "
        "줄을 지우거나 끼워 넣는 것도 연결이 끊겨 바로 드러납니다.", "body")]
F += [P("작품 하나에 여러 세션이 있으면, 각 세션의 마지막 해시를 시간순으로 이어 붙여 "
        "한 번 더 해싱합니다. 그 값이 <font color='#171717'>작품 루트 해시</font>이고 "
        "증명서에 찍힙니다. 기기가 몇 대든 계산이 같습니다.", "body")]

# ══════════════════════════════════════════════════════
F += [CondPageBreak(220)]
F += [rule()]
F += [P("7. 하지 않는 것 — 개인정보 원칙", "h2")]
F += [P("이 서비스는 학생의 화면 앞을 들여다봅니다. "
        "그래서 <font color='#171717'>무엇을 안 하는지가 무엇을 하는지만큼 중요합니다.</font> "
        "아래는 코드 어디에도 있으면 안 되는 것들입니다.", "body")]
F += table(
    ["하지 않는 것", "왜"],
    [["화면 캡처", "한 장으로 모든 것이 새어 나간다. 기록에 필요하지도 않다."],
     ["클립보드 원문", "길이와 해시면 ‘같은 글인지’ 판단에 충분하다."],
     ["어떤 키를 눌렀는지", "횟수만 세면 된다. 무슨 글자인지는 알 필요가 없다."],
     ["AI 답변 내용", "무엇을 물었고 무엇을 받았는지는 학생의 것이다."],
     ["AI 확률 · 점수 · 등급", "판정은 틀릴 때 학생에게 피해를 준다. 그래서 아예 계산하지 않는다."],
     ["학생 간 비교 · 순위", "회고 도구가 경쟁 도구가 되는 순간 아무도 솔직하게 안 쓴다."]],
    [150, 353])
F += callout("기록은 학생 PC 에 먼저 쓰인다",
             "모든 기록은 <font color='#171717'>학생의 컴퓨터에 먼저</font> 저장됩니다. "
             "무엇을 내보낼지는 <font color='#171717'>세션 단위로 학생이 결정</font>합니다. "
             "‘일단 다 서버로 보내고 나중에 고른다’ 가 아닙니다.", GREEN)

# ══════════════════════════════════════════════════════
F += [CondPageBreak(230)]
F += [rule()]
F += [P("8. 무엇을 증명하고, 무엇을 증명하지 못하는가", "h2")]
F += [P("이 구분을 흐리면 서비스 전체가 무너집니다. 먼저 밝히고 갑니다.", "body")]
F += table(
    ["증명함", "증명하지 못함"],
    [["기록이 만들어진 뒤에 고쳐지지 않았다", "AI 를 쓰지 않았다"],
     ["기록된 이벤트의 순서", "이 작업을 누가 했는지"],
     ["기록된 이벤트의 시각", "작업의 품질"],
     ["", "<b>기록되지 않은 시간에 무엇을 했는지</b>"]],
    [251, 252], first_bold=False)

F += [P("가장 큰 한계 — 안 켜고 작업하면 못 잡습니다", "h3")]
F += [P("수집기를 끄고 작업하면 아무것도 남지 않습니다. "
        "<font color='#171717'>서버를 둬도 마찬가지입니다.</font> 안 켰으면 서버에도 안 갑니다. "
        "이건 기술로 막을 수 없습니다.", "body")]
F += table(
    ["대응", "무엇을", "효과"],
    [["자동 실행", "PC 를 켜면 같이 켜진다", "깜빡할 일 자체를 줄인다"],
     ["기록 없는 구간 표시", "파일이 바뀐 시각과 대조해 빈 구간을 드러낸다",
      "숨길 수는 없게 된다"],
     ["자기 신고", "“이 구간은 기록 못 했다”를 학생이 적고, 그 신고도 체인에 남긴다",
      "‘숨긴 것’과 ‘밝힌 것’이 구분된다"]],
    [110, 260, 133])
F += callout("우리가 노리는 것은 완벽한 방어가 아닙니다",
             "1시간짜리 기록을 위조하려면 <font color='#171717'>1시간을 써야 하고, "
             "그러면 그 상당 부분은 진짜 작업입니다.</font><br/><br/>"
             "‘거짓말을 불가능하게’ 만드는 것이 아니라 "
             "<font color='#171717'>‘거짓말의 비용을 진짜 작업의 비용까지 올리는 것’</font>입니다. "
             "이건 숨길 것이 아니라 먼저 말할 내용입니다.", MUTED)

# ══════════════════════════════════════════════════════
F += [CondPageBreak(220)]
F += [rule()]
F += [P("9. 지금까지 확인된 것", "h2")]
F += [P("아래는 <font color='#171717'>실제로 돌려서 측정한 것</font>입니다. 추정이 아닙니다.", "body")]
F += table(
    ["무엇을", "어떻게", "결과"],
    [["변조를 잡아내나",
      "스냅샷 390개에 한 글자씩 10회 무작위 변조",
      f'<font color="{GREEN.hexval()}">10 / 10 위치까지 정확히 검출 · 평균 0.2ms</font>'],
     ["복사→붙여넣기를 잇는가",
      "Claude 에서 복사 → PowerPoint 에 붙여넣기",
      f'<font color="{GREEN.hexval()}">해시 일치 확인 (81자 · 255자)</font>'],
     ["맥·윈도우 양쪽에서 도나",
      "수집기를 두 운영체제용으로 합침",
      f'<font color="{GREEN.hexval()}">맥 실행 확인 · 윈도우 코드 완성</font>'],
     ["권한 없이 되나",
      "맥에서 필요한 권한 조사",
      "앱 이름·클립보드는 권한 불필요 / 키 입력은 ‘입력 모니터링’ 필요"],
     ["사용자 인터뷰",
      "8~10명 대상",
      f'<font color="{RED.hexval()}">미실시 — 다음 단계</font>']],
    [120, 190, 193])
F += [P("맥은 다른 앱의 <font color='#171717'>창 제목을 읽을 수 없습니다</font> "
        "(‘화면 기록’ 권한이 필요해 쓰지 않기로 했습니다). "
        "그래서 맥에서는 브라우저가 어느 탭인지 알 수 없고, 이는 Tier 1 확장이 붙어야 해결됩니다. "
        "윈도우는 권한 없이 창 제목이 읽혀 기록이 더 자세합니다.", "small")]

# ══════════════════════════════════════════════════════
F += [CondPageBreak(200)]
F += [rule()]
F += [P("10. 무엇을 만들 것인가", "h2")]
F += box("최종 형태 — 학생 PC 에 설치되는 프로그램", [
    ("창", "Dashboard · Learn Mode · Proof Mode"),
    ("수집기", "뒤에서 조용히 — 창 · 클립보드 · 키 횟수 · 파일 저장"),
    ("트레이 아이콘", "켜짐 / 꺼짐"),
], BLUE)
F += arrow("인터넷")
F += box("서버 + DB", [("하는 일", "기록 보관 · Proof 체인 검증 · 공유 권한")], MUTED)
F += [sp(12)]

F += table(
    ["", "기능", "우선순위"],
    [["1", "Tier 0 수집기 (창 · 클립보드 · 붙여넣기 · 타자 횟수)",
      f'<font color="{GREEN.hexval()}">Must · 동작 확인됨</font>'],
     ["2", "Learn Mode — 시간 막대 회고 화면", f'<font color="{GREEN.hexval()}">Must</font>'],
     ["3", "해시 체인 · 작품 루트 해시", f'<font color="{GREEN.hexval()}">Must</font>'],
     ["4", "서버 · DB · 기록 보관", f'<font color="{GREEN.hexval()}">Must</font>'],
     ["5", "Proof Mode — 봉인 · 증명서 · 검증 화면", f'<font color="{ORANGE.hexval()}">Should</font>'],
     ["6", "Tier 1 브라우저 확장", f'<font color="{ORANGE.hexval()}">Should</font>'],
     ["7", "앱으로 포장 (창 · 트레이)", f'<font color="{ORANGE.hexval()}">Should</font>'],
     ["8", "외부 앵커 (OpenTimestamps)", "Could"],
     ["9", "설치 파일 (exe / app)", "Could"],
     ["10", "Tier 2 VS Code 확장", f'<font color="{RED.hexval()}">Won’t</font>']],
    [24, 310, 169], aligns=["CENTER"])

F += [P("일정과 역할은 별도 문서에 있습니다 — <font color='#171717'>「Trace 2인 분업 가이드」</font> "
        "(10주 계획 · 담당표 · 관문 3회).", "small")]

F += [CondPageBreak(120)]
F += [rule(INK, 1.0)]
F += [P("한 문장으로", "h3")]
F += [P("<font color='#171717' size='11'>Trace 는 AI 를 썼는지 판정하지 않습니다. "
        "학생이 자기 과정을 볼 수 있게 하고, 필요할 때 그 기록이 고쳐지지 않았음을 "
        "보일 수 있게 합니다.</font>", "lead")]
F += [P("판정은 틀리면 학생을 다치게 하지만, 기록은 틀릴 것이 없습니다. "
        "일어난 일을 남길 뿐이고 해석은 사람이 합니다.", "muted")]


def build():
    out = os.path.join(os.path.dirname(os.path.abspath(__file__)), "Trace_기획안.pdf")
    make_doc(out, "Trace 기획안 v0.3", "기획안 v0.3 · 2026-09-19").build(F)
    print("만들었습니다:", out)
    return out


if __name__ == "__main__":
    build()
